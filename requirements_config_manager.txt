# Config Manager Plugin Dependencies
# Install with: pip install -r requirements_config_manager.txt

# YAML parsing and generation
PyYAML==6.0.1

# Testing dependencies (optional)
pytest==7.4.3
pytest-mock==3.12.0